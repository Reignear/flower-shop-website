// Auto generated code here

export const generateCode = () => {
  const prefix = "PRD";
  const randomNumber = Math.floor(1000 + Math.random() * 9000); // Generates a 3-digit random number
  return `${prefix}-${randomNumber}`;
};
